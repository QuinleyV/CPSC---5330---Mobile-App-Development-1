import SwiftUI

struct ContentView: View {
    var body: some View {
        VStack {
            Image("coaching")
                .resizable()
                .scaledToFit()
                .frame(height: 250)
                .padding()

            Text("Coaching youth basketball is something I really enjoy.")
                .font(.title2)
                .padding()
        }
        .background(Color(.systemGray6))
    }
}

#Preview {
    ContentView()
}
